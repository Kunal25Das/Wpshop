import { Bruno_Ace, Orbitron, Quantico, Audiowide, Tourney, Geostar_Fill } from "next/font/google";

const brunoAce = Bruno_Ace({
    weight: "400",
    variable: "--font-bruno-ace",
    subsets: ["latin"],
});
const orbitron = Orbitron({
    weight: "400",
    variable: "--font-orbitron",
    subsets: ["latin"],
});
const quantico = Quantico({
    weight: "400",
    variable: "--font-quantico",
    subsets: ["latin"],
});
const audiowide = Audiowide({
    weight: "400",
    variable: "--font-audiowide",
    subsets: ["latin"],
});
const tourney = Tourney({
    weight: "400",
    variable: "--font-tourney",
    subsets: ["latin"],
});
const geostarFill = Geostar_Fill({
    weight: "400",
    variable: "--font-geostar-fill",
    subsets: ["latin"],
});

export { brunoAce, orbitron, quantico, audiowide, tourney, geostarFill };